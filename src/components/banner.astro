---
const { backgroundUrl, heading = "", subheading = "" } = Astro.props;
---

<div
  class="flex justify-center items-center text-white text-center bg-cover bg-center w-full"
  style={`background-image: url(${backgroundUrl}); height: 42vh;`}
>
  <div class="bg-black bg-opacity-50 p-6 rounded-lg">
    <h1 class="text-4xl font-bold">{heading}</h1>
    <p class="text-xl mt-2">{subheading}</p>
  </div>
</div>
